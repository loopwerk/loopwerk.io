<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site.name }}</title>
    <updated>{{ site.now|date:"yyyy-MM-dd'T'HH:mm:ss'Z'" }}</updated>
    <link href="{{ site.url }}/articles/feed.xml" rel="self" />
    <link href="{{ site.url }}/articles/" />
    <id>{{ site.url }}/articles/</id>
    {% for page in pages %}
    <entry>
        <title type="html"><![CDATA[{{ page.title }}]]></title>
        <author><name>Kevin Renskers</name></author>
        <link href="{{ site.url }}/{{ page.relativeDestination }}"/>
        <published>{{ page.date|date:"yyyy-MM-dd'T'00:00:00'Z'" }}</published>
        <updated>{{ page.lastModified|date:"yyyy-MM-dd'T'HH:mm:ss'Z'" }}</updated>
        <id>{{ site.url }}/{{ page.relativeDestination }}</id>
        {% for tag in page.metadata.tags %}
        <category scheme="{{ site.url }}/articles/tag/{{ tag|slugify }}/" term="{{ tag }}" label="{{ tag }}" />
        {% endfor %}
        <content type="html" xml:base="{{ site.url }}" xml:lang="en">
            <![CDATA[ {{ page.body }}
            ]]>
        </content>
    </entry>
    {% endfor %}
</feed>
